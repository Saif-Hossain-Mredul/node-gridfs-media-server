# node-gridfs-media-server

A file server using node.js, mongodb, and gridfs. It can stream audio and video files and can directly send other files rather than streaming.

To run it: 

```npm install```

```npm run start```
